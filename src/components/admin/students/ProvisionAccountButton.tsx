/**
 * Superseded by the record-type-agnostic src/components/admin/ProvisionAccountButton.tsx
 * (takes the provision action as a prop so student/parent/coach detail
 * pages can share one implementation). Re-exported here in case
 * anything still imports this path.
 */
export { ProvisionAccountButton } from "@/components/admin/ProvisionAccountButton";
